<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>ورود به حساب</title>

    <!-- فونت -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- فایل CSS -->
    <link rel="stylesheet" href="~/css/login.css" asp-append-version="true" /><!-- with backend -->
</head>

<body>

    <div class="bg"></div>

    <main class="wrap">
        <section class="card grid">

            <div class="hero">
                <h1>خوش آمدید ✨</h1>
                <p>برای ادامه، وارد حساب کاربری خود شوید.</p>
            </div>

            <form class="form" onsubmit="handleSubmit(event)" novalidate>

                <header>
                    <h2 style="margin:0;font-size:18px;">ورود</h2>
                    <a href="#" class="hint">فراموشی رمز؟</a>
                </header>

                <!-- ایمیل -->
                <div class="field">
                    <label for="email">ایمیل یا نام کاربری</label>
                    <div class="input">
                        <svg viewBox="0 0 24 24" width="20">
                            <path fill="currentColor" d="M12 12a5 5 0 1 0-5-5a5 5 0 0 0 5 5m0 2c-5 0-9 2.5-9 5.5V22h18v-2.5C21 16.5 17 14 12 14" />
                        </svg>
                        <input id="email" class="ltr" type="email" placeholder="you@example.com" required>
                    </div>
                    <div class="hint">ایمیل یا نام کاربری را وارد کنید.</div>
                </div>

                <!-- رمز -->
                <div class="field">
                    <label for="password">رمز عبور</label>
                    <div class="input">
                        <svg viewBox="0 0 24 24" width="20">
                            <path fill="currentColor" d="M12 1a5 5 0 0 0-5 5v3H5a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-8a2 2 0 0 0-2-2h-2V6a5 5 0 0 0-5-5m0 2a3 3 0 0 1 3 3v3H9V6a3 3 0 0 1 3-3m0 9a2 2 0 0 1 2 2a2 2 0 0 1-1 1.73V18a1 1 0 0 1-2 0v-2.27A2 2 0 0 1 12 12" />
                        </svg>

                        <input id="password" class="ltr" type="password" minlength="6" placeholder="••••••" required>

                        <!-- چشم -->
                        <button type="button" class="toggle-pass" onclick="togglePassword()">
                            <svg id="eyeIcon" viewBox="0 0 24 24">
                                <path fill="currentColor" d="M12 5c-7 0-10 7-10 7s3 7 10 7s10-7 10-7s-3-7-10-7m0 12a5 5 0 1 1 0-10a5 5 0 0 1 0 10" />
                            </svg>
                        </button>

                    </div>
                    <div class="hint">حداقل ۶ کاراکتر.</div>
                </div>

                <div class="row">
                    <label><input type="checkbox"> مرا به خاطر بسپار</label>
                    <a href="#" class="hint">ساخت حساب جدید</a>
                </div>

                <button class="btn">ورود</button>

                <p class="hint" id="formMsg"></p>

            </form>

        </section>
    </main>

    <!-- فایل جاوااسکریپت -->
    <script src="../js/login.js"></script>

</body>
</html>