<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title> کلینیک زیبایی ملی مهارت</title>

    <!-- فونت -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- فایل CSS -->
    <link rel="stylesheet" href="~/css/signup.css" asp-append-version="true" /> <!-- with backend -->
</head>
<body>

    <div class="bg"></div>

    <main class="wrap">
        <section class="card grid">

            <div class="hero">
                <h1>ثبت‌نام ✨</h1>
                <p>اکانت جدید خود را همین حالا بسازید.</p>
            </div>

            <form class="form" onsubmit="submitReg(event)" novalidate>

                <!-- Fullname -->
                <div class="field">
                    <label for="fullname">نام و نام خانوادگی</label>
                    <div class="input">
                        <svg width="20" viewBox="0 0 24 24">
                            <path fill="currentColor" d="M12 12a5 5 0 1 0-5-5a5 5 0 0 0 5 5m0 2c-5 0-9 2.5-9 5.5V22h18v-2.5C21 16.5 17 14 12 14" />
                        </svg>
                        <input id="fullname" type="text" required placeholder="مثال: علی رضایی">
                    </div>
                </div>

                <!-- Email -->
                <div class="field">
                    <label for="email">ایمیل</label>
                    <div class="input">
                        <svg width="20" viewBox="0 0 24 24">
                            <path fill="currentColor" d="M12 12a5 5 0 1 0-5-5a5 5 0 0 0 5 5m0 2c-5 0-9 2.5-9 5.5V22h18v-2.5C21 16.5 17 14 12 14" />
                        </svg>
                        <input id="email" class="ltr" type="email" required placeholder="you@example.com">
                    </div>
                </div>

                <!-- Password -->
                <div class="field">
                    <label for="password">رمز عبور</label>
                    <div class="input">
                        <svg width="20" viewBox="0 0 24 24">
                            <path fill="currentColor" d="M12 1a5 5 0 0 0-5 5v3H5a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-8a2 2 0 0 0-2-2h-2V6a5 5 0 0 0-5-5m0 2a3 3 0 0 1 3 3v3H9V6a3 3 0 0 1 3-3m0 9a2 2 0 0 1 2 2a2 2 0 0 1-1 1.73V18a1 1 0 0 1-2 0v-2.27A2 2 0 0 1 12 12" />
                        </svg>

                        <input id="password" class="ltr" type="password" minlength="6" required placeholder="••••••">

                        <button type="button" class="toggle-pass" onclick="togglePass()">
                            <svg viewBox="0 0 24 24">
                                <path fill="currentColor" d="M12 5c-7 0-10 7-10 7s3 7 10 7s10-7 10-7s-3-7-10-7m0 12a5 5 0 1 1 0-10a5 5 0 0 1 0 10" />
                            </svg>
                        </button>
                    </div>
                    <div class="hint">حداقل ۶ کاراکتر.</div>
                </div>

                <button class="btn">ثبت‌نام</button>
                <p class="hint" id="regMsg"></p>

            </form>

        </section>
    </main>

    <!-- فایل JS -->
    <script src="../js/js_signup.js"></script>

</body>
</html>
